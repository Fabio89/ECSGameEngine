<UserControl x:Class="Editor.OpenProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Editor"
             xmlns:gameProject="clr-namespace:Editor.GameProject"
             mc:Ignorable="d" Width="720"
             d:DesignHeight="300" d:DesignWidth="800">
    <UserControl.DataContext>
        <gameProject:ProjectOpener x:Name="ProjectOpener"/>
    </UserControl.DataContext>
    <Border  Padding="10">
        <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="Auto" Height="28" Margin="0,10,0,0">
                <TextBlock Text="Path:" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="LightGray" Width="50"></TextBlock>
                <TextBox Width="545" VerticalContentAlignment="Center" Padding="4" Background="#3B3E42" Foreground="LightGray"
                         Text="{Binding ProjectFilePath, UpdateSourceTrigger=PropertyChanged}">
                </TextBox>
                <Button Content="📂" Name="BrowsePathButton" Width="50" Height="28" Margin="5,0,0,0"></Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>